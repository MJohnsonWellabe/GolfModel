# Copy to .env (gitignored) for local LIVE runs, or set as GitHub Actions secrets.
# Open-Meteo (weather) needs NO key.

# DataGolf Scratch Plus API key
DATAGOLF_API_KEY=

# The Odds API key
ODDS_API_KEY=
