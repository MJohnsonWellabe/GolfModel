# Toolchain for generating and previewing the vase lid model.
# Install with:  pip install -r requirements.txt
trimesh>=4.0
manifold3d>=2.3      # robust boolean engine used by trimesh
shapely>=2.0         # 2D profiles (rounded plate)
mapbox_earcut>=1.0   # polygon triangulation for extrude
numpy>=1.24
networkx>=3.0        # required by trimesh for 3MF scene export
lxml>=4.9            # 3MF writer
matplotlib>=3.7      # preview PNG rendering (render_preview.py only)
